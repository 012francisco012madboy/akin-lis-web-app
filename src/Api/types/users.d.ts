

interface User {
  access_token: string;
  id: string;
  refresh_token: string;
}
interface UserData {
  id: string;
  nome: string,
  email: string,
  senha: string,
  tipo: string,
  status: string
}